/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Ini harus mencakup semua file di dalam folder src
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };